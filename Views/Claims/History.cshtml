@model IEnumerable<CMCS.Models.Claim>

@{
    ViewData["Title"] = "History";
}

<h1>Verification History</h1>

@* Table for all claims verified for selected Lecturer profile (Approver functionality)*@

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.claimID)
            </th>
            <th>
                Lecturer Name
            </th>
            <th>
                @Html.DisplayNameFor(model => model.date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th>
                Date Verified
            </th>
        </tr>
    </thead>

    @* Hard-coded values into rows (TEMPORARY SOLUTION!!!!!!!!) *@

    <tbody>
        <tr>
            <td>
                0
            </td>
            <td>
                John Doe
            </td>
            <td>
                15-Sep-25 10:00 AM
            </td>
            <td>
                R 2000
            </td>
            <td>
                Verified
            </td>
            <td>
                16-Sep-25 2:00 PM
            </td>
        </tr>
@* @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.claimID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
} *@
    </tbody>
</table>

<a asp-controller="Approver" class="btn-action" asp-action="Functions">Back to Functions</a>